---

async function addToCart(): Promise<boolean> {
  // Simulate API call
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve(Math.random() < 0.5);
    }, 1000);
  });
}
const inStock = await addToCart();
---

<h1 class="px-4">{inStock? "En Stock": "No Disponible"}</h1>
